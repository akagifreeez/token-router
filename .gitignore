# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/
env/

# Secrets
.env
*.key
config.json

# Run artifacts
results/
data/*.jsonl
*.gguf
# NOTE: anchored to repo root so it ignores a downloaded-weights dir, NOT the
# token_router/models/ source package (a bare "models/" excluded the package and
# broke `pip install git+...`).
/models/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
